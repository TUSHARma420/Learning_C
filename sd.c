#include <stdio.h>

int main()
{
    char source[] = "Symbiosis" 
    char target[20];
    strcpy(target, source);
    printf("in source string=%s", source);
    printf("target string=%s", target);
    return 0;
}